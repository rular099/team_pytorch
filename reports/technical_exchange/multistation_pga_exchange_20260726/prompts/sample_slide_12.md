# Slide 12 样稿生成提示

生成一张完整的 16:9 中文 PowerPoint 页面图像，分辨率适合投影显示。

## 页面身份

- 页码：第 12 页，但页面中不要显示页码。
- 标题：`512-event overfit：拟合能力强，泛化尚未同步`
- 小标签：`Controlled setting · Last checkpoint`
- 页面目标：同时展示 train 与 validation 结果，明确该实验用于验证拟合能力，不是最终系统成绩。

## 视觉风格

- 正式的中国科研答辩风，不是商业路演或宣传海报。
- 米白背景 `#FAFBFC`，浅灰蓝内容底 `#F2F6F8`。
- 炭黑/深灰 `#2F3742` 用于主标题和主要文字，不使用纯蓝标题。
- 低饱和雾霾蓝 `#5B7FA3` 用于 train 和结构线。
- 柔和青绿 `#55A6A0` 用于 validation。
- 低饱和珊瑚色 `#D66F63` 仅用于关键结论和 generalization gap，不使用纯红。
- 边框使用浅灰蓝 `#CCD8E2`。
- 中文无衬线字体，标题粗体，数据清楚，整体为中高信息密度。
- 采用整齐的对齐、细边框、轻微阴影；不要渐变、卡通图标、人物、照片、虚构 logo、水印或多余装饰。

## 版式

1. 顶部：左对齐标题；右侧放小标签。
2. 标题下方放一句醒目的核心判断：
   `训练集 R²≈0.95，但验证集 R²≈0.50：模型容量充足，主要瓶颈是跨事件泛化。`
3. 中部采用左右双栏：
   - 左栏标题：`MAE：train 与 validation`
   - 右栏标题：`R²：train 与 validation`
   - 两栏均使用七组横向 paired bars 或 paired dots，行顺序必须为 rt44、rt45、rt46、rt48、rt49、rt50、rt51。
   - train 使用深蓝，validation 使用青蓝；rt48 用细红框或红色标记强调。
4. 底部放三个简洁证据卡：
   - `训练拟合`：`MAE 0.049–0.076；R² 0.938–0.959`
   - `验证表现`：`MAE 0.297–0.308；R² 0.479–0.512`
   - `泛化差距`：`MAE gap 0.230–0.259；不能由 train 结果直接外推 full-data validation`
5. 最底部结论条：
   `结论：rt44–rt51 已证明结构可拟合；下一步应以 full-data 泛化、strong-PGA 偏差和 unseen-station 为验收重点。`

## 必须准确呈现的数据

所有数字必须逐项准确，不得改写、补造或交换 train/validation：

| Run | Train MAE | Val MAE | Train R² | Val R² |
| --- | ---: | ---: | ---: | ---: |
| rt44 | 0.0491 | 0.3080 | 0.9573 | 0.4787 |
| rt45 | 0.0605 | 0.3054 | 0.9520 | 0.4873 |
| rt46 | 0.0498 | 0.3034 | 0.9587 | 0.4997 |
| rt48 | 0.0491 | 0.2968 | 0.9551 | 0.5122 |
| rt49 | 0.0699 | 0.3018 | 0.9565 | 0.5023 |
| rt50 | 0.0758 | 0.3060 | 0.9375 | 0.4863 |
| rt51 | 0.0610 | 0.3025 | 0.9504 | 0.5049 |

图上保留四位小数值标签。不要添加 rt47。不要把 slope、Corr、RMSE 混入图中。

## 输出约束

- 页面必须是一张完整的演示文稿幻灯片，而不是孤立的信息图。
- 中文必须清晰、无乱码、无错别字。
- 图表数字和行标签必须可读。
- 不显示页码，不显示任何不存在的机构名称或 logo。
